import React from "react";
import classes from "./Chart.module.css";

const Chart = () => {
  return (
    <div className={classes.chart}>
      <h3 className="chartTitle">Chart ทำไงวะไอเหี้ยเย็ดแม่</h3>
    </div>
  );
};

export default Chart;
